# Web Application Collection

Reusable web-development experiments and application foundations imported as
supporting interfaces for intelligent-agent projects.

Each subproject keeps its own dependency manifest where available. Generated builds,
local databases, dependency directories, and large media are excluded. Run and test
subprojects independently rather than installing dependencies at this collection root.
